name: skillorbit_project3
description: SkillOrbit Project #3 - AI Roadmap CLI + Server MVP
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  # HTTP client for Claude API calls
  http: ^1.2.0

  # Server framework
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  shelf_cors_headers: ^0.1.5

  # Utilities
  uuid: ^4.3.0
  meta: ^1.11.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
